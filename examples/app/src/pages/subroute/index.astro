---
import CircleIcon from '@/svgs/circle.svg?component-astro'
const icons = import.meta.glob('@/svgs/*.svg', { as: 'component-astro', eager: true })
console.log(icons)
---

<html lang="en">
  <head>
    <style lang="css">
      .icon {
        color: blue;
      }
    </style>
  </head>
  <body>
    Circle: <CircleIcon class="icon" />
    <!-- {Object.entries(files).map(([path, Icon]) => <Icon class="icon" />)} -->
    <a href="/">Back</a>
  </body>
</html>
